{
  "name": "toddbennison",
  "private": true,
  "type": "module",
  "description": "Personal site for Todd Bennison. Plain HTML, CSS and JavaScript.",
  "scripts": {
    "dev": "wrangler dev --port 3000",
    "format": "prettier --write .",
    "lint": "eslint public/js && prettier --check .",
    "lighthouse": "lhci autorun",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@lhci/cli": "^0.14.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
